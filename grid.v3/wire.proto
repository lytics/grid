syntax = "proto3";

package grid;

message Delivery {
    enum Ver {
        V1 = 0;
    }
    Ver ver = 1;
    bytes data = 2;
    string receiver = 3;
    string codecName = 4;
}

service wire {
    rpc Process(Delivery) returns (Delivery) {}
}